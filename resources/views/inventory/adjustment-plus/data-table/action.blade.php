@if (auth()->user()->can('edit adjustment plus') ||
    auth()->user()->can('delete adjustment plus'))
    <td>
        @can('edit adjustment plus')
            <a href="{{ route('adjustment-plus.edit', $model->id) }}" class="btn btn-success btn-icon btn-circle">
                <i class="fa fa-edit"></i>
            </a>
        @endcan

        @can('detail adjustment plus')
            <a href="{{ route('adjustment-plus.show', $model->id) }}" class="btn btn-info btn-icon btn-circle">
                <i class="fa fa-eye"></i>
            </a>
        @endcan

        @can('delete adjustment plus')
            <form action="{{ route('adjustment-plus.destroy', $model->id) }}" method="post" class="d-inline"
                onsubmit="return confirm('Yakin ingin menghapus data ini?')">
                @csrf
                @method('delete')

                <button class="btn btn-danger btn-icon btn-circle">
                    <i class="ace-icon fa fa-trash"></i>
                </button>
            </form>
        @endcan
    </td>
@endif
