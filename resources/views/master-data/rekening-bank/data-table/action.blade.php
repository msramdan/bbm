@if (auth()->user()->can('edit rekening') ||
    auth()->user()->can('delete rekening'))
    <td>
        @can('edit rekening')
            <a href="{{ route('rekening-bank.edit', $model->id) }}" class="btn btn-success btn-icon btn-circle">
                <i class="fa fa-edit"></i>
            </a>
        @endcan

        @can('delete rekening')
            <form action="{{ route('rekening-bank.destroy', $model->id) }}" method="post" class="d-inline"
                onsubmit="return confirm('Yakin ingin menghapus data ini?')">
                @csrf
                @method('delete')

                <button class="btn btn-danger btn-icon btn-circle">
                    <i class="ace-icon fa fa-trash"></i>
                </button>
            </form>
        @endcan
    </td>
@endif
